name: 'C++ CI'

on:
  push

jobs:
  build: 
    permissions: write-all
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Init
        run: cmake . -DPATCH_VERSION=${{ github.run_number }}

      - name: Check
        run: dir

      - name: Build
        run: cmake --build . --config Release

      - name: Pack
        run: cpack --config CPackConfig.cmake

      - name: Run check
        if: runner.os == 'Windows'
        run:  ./Release/helloworld_cli.exe

      - name: Run check
        if: runner.os == 'Linux'
        run: ./helloworld_cli

      - name: check
        run: dir

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          path: helloworld-*.zip
            
      
  release:
    needs: build
    runs-on: ubuntu-latest
    permissions: write-all
    steps:
      - name: Download
        uses: actions/download-artifact@v4    
        with:
          path: artifact

      - run: ls -R

      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifact/lab-Linux/helloworld-0.0.9-Linux.deb
            artifact/lab-Windows/helloworld-0.0.9-win64.zip
          tag_name: 1.0.${{ github.run_number }}
          body: "Release 1.0.${{ github.run_number }}"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
